name: Telegram Recovery Heartbeat v2

on:
  workflow_dispatch:

permissions:
  actions: write
  contents: read

concurrency:
  group: telegram-recovery-v2
  cancel-in-progress: true

jobs:
  recover:
    if: ${{ false }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Frozen repository guard
        run: echo "Repository automation is frozen."
