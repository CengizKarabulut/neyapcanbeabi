name: Telegram Market Loop
run-name: "Telegram Market Loop · frozen"

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read

concurrency:
  group: telegram-market-loop
  cancel-in-progress: true

jobs:
  loop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Frozen repository guard
        run: echo "Repository automation is frozen."
